<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <base href="{{ '/' | htmlBaseUrl }}">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" href="/assets/favicon.ico" type="image/x-icon">
  <title>{{ title }} | Lyiar</title>
  <link rel="stylesheet" href="/css/index.css">
</head>
<body>
  {% include "header.njk" %}

  <main>
    {# O {{ content | safe }} injeta o conteúdo da página que usar este layout #}
    {{ content | safe }}
  </main>

  {% include "footer.njk" %}
</body>
</html>